# 流行歌和弦面板 MVP

这个面板把 C 大调最基础的六个和弦做成六列，让使用者先从声音和空间关系认识和声。底层仍只记录归一化触点 `{x, y}`，不在 Loop 中保存和弦 ID。

三和弦的实际 voicing 会在高八度重复根音，形成四声部 `[根、五、根↑8、三↑8]`；七和弦使用 `[根、五、七、三↑8]`。两者拥有相同声部数，因此持续拖动和 Loop 回放可以在三和弦与七和弦之间即时切换。

所有按钮都保留原来的默认 voicing，不为了下一和弦重排源和弦。唯一的上下文变化是核心 `I` 有两套固定排列：通常使用低音区的 `[C3, G3, C4, E4]`；从 `V / V7 / ♭VII` 进入时使用解决型 `[C4, E4, G4, C5]`，让 `V` 的最高声部 `B4 → C5`，同时让 `♭VII → I` 的四个声部保持小距离移动。连续按 `I` 会保持解决型；按过任何其他和弦后，普通进入 `I` 会恢复默认型。Loop 仍只存触控位置，回放时按相同的和弦顺序重建这个状态。

## 布局规则

- 从左到右严格排列 `I / ii / iii / IV / V / vi`，六个三和弦同样宽、同样高；暂不提供较少见且不稳定的 `vii°`。
- 色彩行不再为了填满六列而硬塞和弦：I 列上下分成 `Isus4 / Isus2`，IV 列保留调外功能色彩 `iv`；V 列上 1/3 是 `♭VI`、下 2/3 是 `Vsus4`，vi 列上方是 `♭VII`，由此形成 `♭VI → ♭VII → I` 的相邻路径。ii、iii 上方的空位仍安全回落到对应核心三和弦。
- 每列正下方是同根的调内七和弦：`Imaj7 / ii7 / iii7 / IVmaj7 / V7 / vi7`。
- `V/IV / V/V / V/vi` 分别嵌在 `I / ii / iii` 三个核心按钮的顶部，高度为核心区的五分之一；其根音与所在列一致，并直接标出解决目标。
- `G/B / Em/G / C/E` 不常驻为第二套按钮：只有前一步能形成常见低音路线时，`/B / /G / /E` 才会出现在目标三和弦的下方 1/3，上方 2/3 仍播放原位和弦。卡农路径 `C–G/B–Am–Em/G–F–C/E–F–G` 固定形成低音 `C–B–A–G–F–E–F–G` 与最高音 `E–D–C–B–A–G–A–B`；末尾从低位 `IV` 进入的 `V` 也保持低位，普通 `IV → V` 则不改变原排列。`Dm–C/E–F` 与 `Am–G/B–C` 保留各自的上行版本。按下转位入口后，唯一符合当前路径的下一目标使用强节拍提示。
- `♯ii°7 / ♯IV°7 / ♯V°7` 使用与 secondary dominant 高度相同的宽度，从核心区下边缘向下拉长，并覆盖七和弦行顶部约 1/5；它们分别跨在 `ii–iii / IV–V / V–vi` 之间，作为半音上行的窄桥。
- 中间三和弦区更高，仍是最容易触发的主操作区；上下移动只改变同一个根音的和弦色彩。
- 演奏一个和弦后，常见后续和弦会出现细描边。`V/IV / V/V / V/vi / ♭VI / ♭VII` 以及三个经过减七和弦之后的目标，使用随 BPM 从边缘向内闪动的强提示；建议只影响视觉，不改变录制数据。

## 色彩和弦的典型上下文

| 和弦 | C 大调 | 优先尝试 | 听感/作用 |
|---|---|---|---|
| `Isus4` | Csus4 | `Isus4 → I` | 三音暂时升到四音，再落回 I；有明确的“悬住—落地”练习 |
| `Isus2` | Csus2 | 可替换 `I`、再回 `I` | 比 sus4 更开放，解决方向较弱；作为并排听辨用的次级选项 |
| `iv` | Fm | `IV → iv → I`、`iv → I` | 唯一默认的半音外借用；降六级音向五级音下行，回 I 的方向明确 |
| `Vsus4` | Gsus4 | `Vsus4 → V → I` | 暂时拿掉三音，以四音制造挂留，再回到 V |
| `♭VI` | A♭ | `♭VI → ♭VII → I` | 平行小调借用路径的起点；连续根音上行让去向比孤立的借用和弦更容易听懂 |
| `♭VII` | B♭ | `♭VII → I` | 以全音下方的大三和弦回到主和弦，带来明显的 Mixolydian／摇滚式回归 |
| 半音 diminished | D♯dim7 / F♯dim7 / G♯dim7 | `ii → ♯ii°7 → iii` 等 | 根音与多个声部以半音接向右侧调内和弦，因此只作为按钮之间的桥 |

## 研究依据与边界

- 柴田阳介（九州大学，2019）分析了九位日本艺人的 91 首完整歌曲与官方乐队谱。`Isus4` 的使用有明显艺人差异，但出现后几乎都走向 I，因此它适合成为“听解决”的教学按钮，而不是一个无条件替换 I 的色彩。
- 同一研究里较突出的 9th 类型是 `vi m7(9) / i m7(9) / ♭VIIadd9 / IVadd9 / Iadd9`。这里的“较突出”只表示它们在 **9th 子样本内部** 的合计值较高，不能推出它们在全部 91 首歌里普遍，更不能推出 `IVadd9` 比其他功能变化更值得占据默认面板。
- 研究方法是把乐谱上印刷的 chord symbol 原样录入，再统计其前后和弦；它没有系统区分第九音来自和弦伴奏、键盘内声、pad，还是主旋律。作者也在限制中承认，单看 chord symbol 会漏掉 voicing，官方谱仍可能有错误或简化。因此这些数据只能证明某些谱面把纵向声音标成了 add9，不能证明伴奏面板应当主动弹出第九音。
- 木下的 J-pop 旋律研究给出了更直接的反例：在 `Am → Dm → G → C` 上持续唱同一个 C，它依次会被解释为三音、七音、十一音、根音。这里的 11th 明确来自“旋律相对和弦”的关系，而不是伴奏必须弹一个 `G11`。由此可见，流行歌里的延伸音经常是旋律与和弦共同产生的纵向结果，不应全部固化进伴奏和弦。
- 上原等人的 J-pop 进行识别研究在匹配九种常见进行时，主动忽略复杂 tension 和 slash-bass，只保留根音、大小调与少量七和弦信息。这从方法上支持把“进行/功能层”和“voicing/tension 层”拆开，而不是让每个 tension 与功能和弦争夺同一层级。
- `9 / 11 / 13` 和 altered tension 全部延后到独立的 tension／voicing 层；该层应当成套设计，让各根音可用的 tension、旋律避让和声部进行保持平衡。
- 成功大学关于旋律与和弦匹配的论文以张惠妹《记得》、王菲《红豆》、周杰伦《千里之外》等为例，指出华语抒情歌大量共享卡农进行；这里最值得新手先掌握的是 I–V–vi–iii–IV–I–ii–V 的功能路线，而不是为每一级添加 tension。
- 范雅惠整理 2005–2014 年 287 首 Hit FM 中文歌曲后列出的九类常见进行，公开摘要中的和弦几乎全部来自 I–vi 的调内骨架，复杂处主要是 `G/B、Em/G、C/E` 这类低音转位。它支持当前“先学调内功能与低音路线”的次序，但没有发表调外和弦类型的频率表，因此不能拿来证明 `iv`、`♭VII` 或 add9 谁更常见。
- 针对国语爱情抒情歌的研究还会同时分析配器、调性、和声、和声节奏、旋律、节奏与超节奏；另一项国语抒情慢歌研究则发现副歌的情绪强调常来自织度、力度、速度和调性变化。换言之，“芭乐感”并不等于堆叠更多和弦延伸音。
- 华语芭乐常用的级进低音会进一步产生 `V/7`、`iii/5`、`I/3` 等转位/斜线和弦。它们改变的是低音线而非同根色彩，因此本版只把它们做成由前一和弦触发的局部入口，不挤占色彩行，也不要求用户同时操作两套全局控制。
- `♭VII / ♭III / ♭VI` 是真实而重要的摇滚/调式借用群组，会把听感推向平行小调或 Mixolydian。本版只开放方向最明确的 `♭VI → ♭VII → I` 路径，不加入较容易扩散调性感的 `♭III`。
- Secondary dominant 会暂时把另一个和弦听成主音，功能是“主音化”而不只是染色。因此只把 `V/IV / V/V / V/vi` 作为核心按钮中的窄功能入口，不再把它们与色彩和弦并列。
- 色彩行除 `iv` 外全部只使用 C 大调音。`iv` 被保留，是因为它不只是同一功能的加音 voicing：它引入调外的 ♭6，并通过 `iv → I` 产生清楚的半音解决。
- `sus2` 在这批 J-pop 研究里没有得到与 `sus4` 同等的频率支持；保留它是为了响应产品目标，让用户直接比较两种挂留音色，而不是声称它比 `Iadd9` 更普遍。
- 语料库会受到年代、流派和标注规范影响；add/sus 的具体记法也常被简化，所以这里选择的是保守、易听懂的教学词汇，不声称覆盖所有 J-pop 或华语芭乐。

## 延伸音在文献里实际上怎么分析

| 类型 | 这份 91 首 J-pop 研究发现了什么 | 对产品的含义 |
|---|---|---|
| `9 / add9` | 9th 比其他 tension 分布到更多艺人与更多根音；但它通常仍按基本和弦的功能使用，而且周围更常出现七和弦、其他 tension 或 hybrid chord，而不是一圈朴素三和弦 | 它像是“已经选择复杂编配语汇之后的 voicing”，不应冒充第一层功能色彩 |
| `11` | 全语料只有 13 条相关进行；几乎都处在本来就复杂的上下文。有些艺人只在 C 段等特殊段落使用，用来加强弱进行和漂浮感 | 做成高级、段落性音色，不做每一级的对称默认选项 |
| `13` | 只有星野源的两例，且都在 dominant motion 中；13th 音属于下一和弦，作者把它解释为对后续和弦的预备 | 必须和“下一步去哪里”绑定，不能做静态颜色按钮 |
| altered tension | 几乎都用于 dominant motion；常见逻辑是提前出现下一和弦的构成音，或以半音解决 | 应属于 dominant/voice-leading 工具，不属于大调默认色彩行 |

这个结果也解释了所谓“tension 不平衡”：如果只给 I、ii、iii、vi 随手加 9/11，界面是在比较不等价的对象。有的音是伴奏 voicing，有的是旋律造成的纵向 tension，有的是下一和弦的先现音，有的则只有在 dominant motion 中才成立。

## 还有哪些像 iv 的调外色彩

这里把 secondary dominant 排除，因为它们在听觉上主音化目标和弦；也把 add9 排除，因为它不改变和弦在调内的功能身份。剩下的候选按“证据强度 + 新手是否仍能听见主调”排序：

| 候选 | 典型用法 | 证据与判断 | 面板建议 |
|---|---|---|---|
| `♭VII` | `♭VII → I`、`♭VII → IV` | 柴田的语料里它频繁借入大调，后面常去 tonic；功能介于 subdominant 与 dominant。它是 `iv` 之外最有资格进入下一版的**单个**调外色彩，但会比 `iv → I` 更偏摇滚／Mixolydian | 已放在 vi 上方，并以强提示指向 I |
| `♭VI → ♭VII → I` | 三个大三和弦整体上行 | 91 首语料里 `♭VI` 最常走 `♭VII`，而 `♭VI → ♭VII → I` 在大调中也频繁出现；但连续出现 ♭6、♭7 会明显唤起平行小调／Aeolian | 已做成 V、vi、I 相邻列间的可探索路径，避免呈现为两个没有上下文的孤立大按钮 |
| `♭III` | 常与 `♭VI / ♭VII / IV` 形成 rock/modal 群组 | 它是真实的平行小调借用，但作为单独和弦没有 `iv` 那样清楚的半音回归；更容易让根音中心移动 | 延后到完整的“摇滚／平行小调借用”预设 |
| 半音 passing dim | `ii → ♯ii°7 → iii`、`IV → ♯IV°7 → V`、`V → ♯V°7 → vi` | 柴田发现大调里的这三类 dim 常以半音接向相邻调内和弦，但使用量有强烈艺人偏差，尤其集中在星野源 | 已做成**两列之间的窄桥**；默认提示较弱，避免把语料偏好误当成所有流行歌的必经路线 |
| `iiø7` | `iiø7 → V → i` | 虽然 m7♭5 分布广，但该研究里的 `iiø7` 前面多是小调 tonic，后面几乎全去 V，本质是小调 ii–V | 不作为大调色彩默认项；留给小调／爵士层 |
| aug | 多用于固定半音 cliché | 研究中例子很少，作者判断其用法非常受限，主要依赖具体 voicing 的半音线条 | 不做独立按钮；以后作为 voice-leading 模板 |

因此本版把 `iv` 之外的新增内容限制为两类“有路径的调外和弦”：`♭VI → ♭VII → I` 和三个半音 diminished 窄桥。它们没有扩大成完整借用和弦库，也没有与六个核心和弦争夺同样面积。

参考资料：

- 柴田陽介，[《日本のポピュラー音楽の複雑な和声：自動分析プログラムの構築》](https://api.lib.kyushu-u.ac.jp/opac_download_md/2231541/shibata_2019.pdf)，九州大学，2019。
- 木下和彦，[《中学校音楽科における J-POP を用いた旋律創作の実践》](https://www.jstage.jst.go.jp/article/jjomep/13/2/13_66/_pdf)，2016。
- 上原美咲、伊藤貴之、高塚正浩，[《コード進行に注目した J-POP 音楽の可視化》](https://www.jstage.jst.go.jp/article/artsci/15/4/15_177/_pdf)，2016。
- 林煒婷，[《基於樂曲譜面資訊之和弦進行與旋律匹配》](https://researchoutput.ncku.edu.tw/zh/studentTheses/chord-progression-and-melody-matching-based-on-music-score/)，國立成功大學，2020。
- 范雅惠，[《探討華語流行音樂的和弦組合模式——以 Hit FM 歌曲排行榜為例》](https://ndltd.ncl.edu.tw/cgi-bin/gs32/gsweb.cgi/login?o=dnclcdr&s=id%3D%22105TTU05716023%22.&searchmode=basic)，大同大學，2017。
- 許汀昀，[《當代國語流行歌曲的時間性分析——以華語地區創作型男歌手作品為例》](https://www.airitilibrary.com/Article/Detail/U0001-1508201815554800)，國立臺灣大學，2018。
- 陳容姍，[《「主歌－副歌」的曲式及聆聽行為研究——以臺灣的國語抒情慢歌為例》](https://tdr.lib.ntu.edu.tw/handle/123456789/15976)，國立臺灣大學，2012。
- Trevor de Clercq 与 David Temperley，[A Corpus Analysis of Rock Harmony](https://rockcorpus.midside.com/2011_paper.html)，2011。仅用于交叉验证 `♭III / ♭VI / ♭VII` 在 rock 中成群出现，不作为 J-pop／华语芭乐频率的主证据。
