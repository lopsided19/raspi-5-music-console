{
  "name": "music-console",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5174 --strictPort",
    "build": "vite build",
    "chords:reference": "node scripts/generate-chord-reference.mjs",
    "test": "node --test"
  },
  "devDependencies": {
    "vite": "^7.1.1"
  }
}
